{
  "job_id": "otel-demo-error-rate",
  "description": "High count of failed-outcome spans per service, excluding flagd's own streaming RPC noise",
  "analysis_config": {
    "bucket_span": "5m",
    "detectors": [
      {
        "function": "high_count",
        "partition_field_name": "resource.attributes.service.name",
        "detector_description": "unusually high count of failed spans per service"
      }
    ]
  },
  "data_description": {
    "time_field": "@timestamp"
  },
  "analysis_limits": {
    "model_memory_limit": "64mb"
  }
}
